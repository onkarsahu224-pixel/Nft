services:
  - type: web
    name: nft-bazaar
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python nft_bazaar.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
